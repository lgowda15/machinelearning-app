# Logistic Regression (reference implementation)

Not an allotted topic. This folder is the pattern every group follows:
a BaseModel subclass in model.py, tests in test.py, pinned dependencies,
and this README. Read it alongside CODING_STANDARDS.md.
