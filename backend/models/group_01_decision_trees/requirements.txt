# Group 01 - Decision Trees (ID3, CART, CHAID, Sparse Oblique)
# Python 3.12.x, CPU only. Every dependency is open source and pinned.

# Runtime
scikit-learn==1.9.0
numpy==2.2.6
scipy==1.15.3

# Test-only (pytest + pytest-cov, as required by Section 12)
pytest==8.3.5
pytest-cov==6.0.0
